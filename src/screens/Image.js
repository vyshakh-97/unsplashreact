import {React, useContext} from 'react'
import { ImageContext } from '../context/Contextprovider'

const Image = () => {
    const {imageinfo} = useContext(NewsContext);
    return (
        <div>
            
        </div>
    )
}

export default Image
